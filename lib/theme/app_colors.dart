import 'package:flutter/material.dart';

class AppColors {
  // Core brand
  static const primary = Color(0xFF0A6B4E);   // Emirati Green
  static const secondary = Color(0xFF003A8F); // RTA Blue
  static const accent = Color(0xFFE5B84B);    // Gold

  // Backgrounds
  static const background = Color(0xFFF6F8F7);
  static const surface = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF1E1E1E);
  static const textSecondary = Color(0xFF5F6368);

  // Status
  static const success = Color(0xFF1DBF73);
  static const warning = Color(0xFFF4A261);
  static const error = Color(0xFFD64545);
}
